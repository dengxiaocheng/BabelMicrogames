#!/usr/bin/env bash
set -euo pipefail

repo_root=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)
cd "$repo_root"

go run ./cmd/babel-dev check-docs-consistency
go run ./cmd/babel-dev check-docs-sync-guard --staged
go run ./cmd/babel-dev check-requirement-assets
